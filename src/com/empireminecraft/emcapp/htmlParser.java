package com.empireminecraft.emcapp;

public class htmlParser {
	/*String HTML = null;
	String parsedHtml = null;
	public htmlParser(String html){
		HTML = html;
	}
	public String ParseHTML(){
		for(int i = 0; i<HTML.length(); i++){
			if(HTML.regionMatches(i, "<br />", 0, 6)){
				parsedHtml += "/n";
				i += 5;
			}
			//else if(HTML.regionMatches(i, string, start, length))
		}
	}*/
}
